F4 SCOPE FIDELITY REVIEW (RERUN)
Date: 2026-03-27
Plan: .sisyphus/plans/planning-poker.md
Mode: Read-only audit focused on prior rejection reasons and flagged carry-over items

SUMMARY
- Re-checked the six explicit rejection reasons from the prior F4 review.
- Read `src/main.tsx`, `src/pages/Room.tsx`, `src/components/JiraImportModal.tsx`, `convex/rooms.ts`, `convex/voting.ts`, `convex/jira.ts`, and `package.json`.
- Result: all 6 critical fixes are now present, but one previously flagged item (T10) remains a real scope violation.

CRITICAL FIX VERIFICATION
1. T13 Room placeholders: FIXED
   - `src/pages/Room.tsx:189-215` now renders real voting content.
   - Placeholder strings `Voting Area (Task 14)` and `Results Area (Task 15)` are gone.

2. T14 CardDeck integration: FIXED
   - `src/pages/Room.tsx:197-205` renders `<CardDeck />` when `room.status === "voting"`.

3. T15 ResultsPanel integration: FIXED
   - `src/pages/Room.tsx:207-214` renders `<ResultsPanel />` for the active task.
   - The enclosing condition is `(room.status === "voting" || room.status === "revealed")`, so results remain available in both states.

4. T16 JiraImportModal contract mismatch: FIXED
   - `src/components/JiraImportModal.tsx:36` uses `useMutation(api.jira.importFromJira)`.
   - No `@ts-expect-error` or `@ts-ignore` suppression remains.

5. T19 Convex provider wiring: FIXED
   - `src/main.tsx:11-16` wraps the app with `<ConvexProvider client={convex}>`.
   - This restores the realtime/root-provider wiring that was previously missing.

6. T21 preview script: FIXED
   - `package.json:11` now includes `"preview": "vite preview"`.

FLAGGED CARRY-OVER ITEMS (REASSESSED)
T7 getRoom: ACCEPTABLE DEVIATION
- `convex/rooms.ts:30-38` returns the room document only.
- `src/pages/Room.tsx:26-42` separately subscribes to room, tasks, participants, and vote status in parallel.
- This still delivers the required room screen behavior and is a reasonable Convex realtime design choice rather than a missing feature.

T10 advanceToNextTask: VIOLATION
- `convex/voting.ts:216-224` loads votes for `tasks[nextTaskIndex]` and deletes them before patching the room to the next task.
- The earlier plan decision was to keep votes for history.
- This is not a naming/style difference: it is behavior that explicitly contradicts the intended vote-retention rule.

T11 importFromJira naming: ACCEPTABLE DEVIATION
- `convex/jira.ts:57` exports `importFromJira` instead of `triggerJiraImport`.
- The mutation still updates import status and schedules `_importFromJiraInternal`, so the functional flow exists.
- The name difference alone is not a meaningful scope miss.

ORIGINAL USER REQUIREMENTS
- Jira remains read-only in the backend flow.
- Username/session identity remains localStorage-based.
- Convex is now actually wired at the root via `ConvexProvider`.
- Poker cards remain configurable via room `cardSet`.
- Hours estimation and average estimation remain implemented.
- Reveal does not require all votes; any participant can show results.
- Frontend remains React + React Router without SSR.
- Realtime/reconnect consideration is materially improved because Convex is now mounted at the app root.

CONCLUSION
- Previous rejection reasons addressed: 6/6
- Remaining meaningful scope issue: 1 (T10 vote-history deletion on task advance)

VERDICT: REJECT
Reason: the integration fixes resolved the prior blockers, but `advanceToNextTask` still deletes votes for the destination task, which conflicts with the explicit vote-history requirement and remains a substantive scope fidelity issue.
