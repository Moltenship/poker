F2: Code Quality Review — Quality Evidence
Generated: Fri Mar 28 2026

========================================
1. @ts-ignore / @ts-expect-error
========================================

Count: 0 occurrences in src/ and convex/ (excluding _generated)

No suppressed TypeScript errors found anywhere in production or test code.
RESULT: CLEAN

========================================
2. "as any" casts
========================================

PRODUCTION SOURCE FILES (non-test):
  src/components/CardDeck.tsx:24          — (api as any).voting.castVote
  src/components/IdentityFlow.tsx:38      — (api as any).participants.joinRoom
  src/components/IdentityFlow.tsx:39      — (api as any).participants.takeoverSession
  src/components/IdentityFlow.tsx:41      — (api as any).participants.listRoomParticipants
  src/components/SessionKickedBanner.tsx:26 — (api as any).participants.getParticipants
  src/pages/Home.tsx:21                   — (api as any).rooms.createRoom
  src/pages/Room.tsx:20                   — (api as any).participants.joinRoom
  src/pages/Room.tsx:21                   — (api as any).participants.takeoverSession
  src/pages/Room.tsx:22                   — (api as any).voting.startVoting
  src/pages/Room.tsx:24                   — (api as any).rooms.getRoom
  src/pages/Room.tsx:27                   — (api as any).tasks.getTasksForRoom
  src/pages/Room.tsx:32                   — (api as any).participants.getParticipants
  src/pages/Room.tsx:38                   — (api as any).voting.getVoteStatus
  convex/jira.ts:22-27                   — internalJira internal API type workarounds

  Pattern: ALL production "as any" casts follow one of two patterns:
  (a) "(api as any).module.function" — Convex hook accepts typed FunctionReference,
      but useSessionMutation/useSessionQuery are custom wrappers without full generic
      propagation. This is a known Convex type interop limitation.
  (b) "internalJira.module: any" — Convex internal API references that require
      dynamic type augmentation at runtime.

  ASSESSMENT: Concerning but systematic. Not random type suppression — a consistent
  workaround for one root type incompatibility. The fix would be to type 
  useSessionMutation/useSessionQuery to accept FunctionReference<any> properly.

TEST FILES (informational, not penalized):
  29 occurrences across convex/__tests__/voting.test.ts and jira.test.ts —
  all "fn as any" for convex-test harness API which requires typed references.
  8 occurrences across src test files — test setup mocking patterns.

Total production "as any": ~14 occurrences
Total in tests: ~37 occurrences

RESULT: MODERATE CONCERN (systematic workaround, not random type suppression)

========================================
3. console.log in production code
========================================

Count: 0 console.log occurrences

HOWEVER, console.error IS used (10 occurrences):
  src/components/IdentityFlow.tsx:81    — catch(error) { console.error("Failed to join room:", error) }
  src/components/IdentityFlow.tsx:103   — catch(error) { console.error("Failed to take over session:", error) }
  src/components/TaskListManager.tsx:40 — catch(err) { console.error("Failed to set current task:", err) }
  src/components/TaskListManager.tsx:49 — catch(err) { console.error("Failed to delete task:", err) }
  src/components/CardDeck.tsx:28        — castVote(...).catch(console.error)
  src/pages/Home.tsx:44                 — catch(e) { console.error("Failed to load recent rooms", e) }
  src/pages/Home.tsx:93                 — catch(error) { console.error("Create room error:", error) }
  src/pages/Room.tsx:67                 — catch(error) { console.error("Failed to restore room identity:", error) }
  src/pages/Room.tsx:102                — catch(err) { console.error("Failed to start voting:", err) }
  src/pages/Room.tsx:124                — catch(error) { console.error("Failed to reclaim session:", error) }

ASSESSMENT: All 10 are error-handling console.error calls in catch blocks —
acceptable production logging. No debug console.log spam. No console.warn.
RESULT: CLEAN (error logging only, expected in production error handlers)

========================================
4. TODO / FIXME / HACK / XXX
========================================

Count: 0 occurrences in src/ or convex/ (production or test code)

RESULT: CLEAN

========================================
5. Empty catch blocks
========================================

Count: 0 occurrences

The one catch block without a variable:
  src/hooks/useIdentity.ts:34 — "catch {" (no variable, but removes corrupt storage)
  This is intentional pattern — catch without binding to discard the parse error.

RESULT: CLEAN

========================================
6. Hardcoded "customfield_" strings
========================================

Count: 0 in production convex/ code

3 occurrences in convex/__tests__/jira.test.ts (test fixtures only):
  Line 63: id: "customfield_10016" — mock field data
  Line 162: fields: [..., "customfield_10016"] — test assertion
  Line 174: fields: [..., "customfield_10016"] — test assertion

The production jira.ts dynamically discovers the story points field ID by querying
the Jira API for fields and finding by name — no hardcoded field IDs. Correct design.

RESULT: CLEAN

========================================
7. SPOT-CHECK: 5 Key Files
========================================

FILE 1: convex/voting.ts (231 lines)
  ✓ parseCardValue handles "½" special case + NaN — correct
  ✓ calculateAverage filters non-numeric (? / ☕) votes properly
  ✓ castVote validates task existence, room status, participant ownership
  ✓ getVoteStatus properly checks all participants vs voted set
  ✓ revealVotes, resetVoting, advanceToNextTask all have proper guard clauses
  ✗ MINOR: getSortedTasksForRoom(ctx) typed as ctx: { db: { query: Function } }
    — uses Function type (overly loose) instead of proper Convex ctx type.
    This is a workaround to share a helper between query and mutation contexts.
  ✓ No stubs, no dead code
  VERDICT: CLEAN (minor type looseness in internal helper)

FILE 2: src/components/ResultsPanel.tsx (133 lines)
  ✓ Conditionally skips query with "skip" when taskId null or not revealed
  ✓ Handles null voteResults gracefully (fallback displays)
  ✓ findNearestCard called only when average exists — no null-deref risk
  ✓ formattedVotes handles unknown participants ("Unknown" fallback)
  ✓ Key on list uses index (i) — acceptable for immutable result list
  ✓ No stubs, no dead code
  VERDICT: CLEAN

FILE 3: src/hooks/useIdentity.ts (71 lines)
  ✓ SSR-safe (typeof window check)
  ✓ try/catch on JSON.parse removes corrupt localStorage entry
  ✓ useMemo/useCallback with correct deps
  ✓ Validates shape of parsed object before using
  ✓ TypeScript: uses Partial<StoredIdentity> + manual type narrowing
  VERDICT: CLEAN

FILE 4: convex/jira.ts (227 lines)
  ✓ Pagination via nextPageToken loop — handles large Jira projects
  ✓ Discovers story points field dynamically (no hardcoded ID)
  ✓ adfToPlainText handles Atlassian Document Format recursively
  ✓ Error handling: catch sets importError on room, doesn't crash
  ✓ Environment variables checked before use
  ✗ MODERATE: jiraGlobals typed as globalThis & { ... } workaround
    Lines 20-29: internalJira cast uses multiple "any" types for internal API
    This is a Convex limitation — internal references can't be typed through
    the generated API object without these workarounds.
  ✗ MINOR: json() return typed as Promise<any> in JiraFetchResponse (line 8)
    This is intentional since different endpoints return different shapes.
  ✓ No stubs or placeholder implementations
  VERDICT: ACCEPTABLE (known Convex typing workarounds)

FILE 5: src/components/CardDeck.tsx (52 lines)
  ✓ Correct disabled state logic (roomStatus !== "voting")
  ✓ castVote called with correct args
  ✓ useCallback with correct deps array
  ✗ Line 24: (api as any).voting.castVote — same systematic cast pattern
  ✗ Line 28: castVote(...).catch(console.error) — adequate error handling
  ✓ No stubs, no dead code, clean render
  VERDICT: CLEAN

========================================
SUMMARY
========================================

@ts-ignore/expect-error:  0 occurrences
as-any (production only): 14 occurrences (systematic, one root cause)
as-any (tests only):       37 occurrences (expected Convex test harness pattern)
console.log:               0 occurrences
console.error:            10 occurrences (error handlers — acceptable)
TODO/FIXME/HACK:           0 occurrences
Empty catch blocks:        0 occurrences
Hardcoded customfield_:    0 in production (3 in tests — fixtures)
Spot checks passing:       5/5 (with minor notes)

Critical issues: NONE
Moderate issues:
  1. Systematic "as any" in useSessionMutation/useSessionQuery wrappers
     (root cause: custom hook missing proper generic type parameters)
  2. Function type in getSortedTasksForRoom ctx parameter

Failing test:
  IdentityFlow > "asks for confirmation before taking over a returning user session"
  Root cause: shadcn Select combobox portal not rendering role="option" in JSDOM
  when using user.click() without awaiting the open state. The dropdown's
  aria-expanded stays "false" — options never appear in the accessible tree.
  This is a real bug in the test, not a flaky failure.
