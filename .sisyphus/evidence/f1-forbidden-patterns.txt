F1 FORBIDDEN PATTERNS AUDIT
Date: 2026-03-27

COMMAND RESULTS
1. No auth libraries
   - Command: grep -r "clerk\|auth0\|firebase.auth\|nextauth" src/ convex/ --include="*.ts" --include="*.tsx" -i
   - Result: 0 matches

2. No custom WebSocket logic
   - Command: grep -r "new WebSocket\|WebSocket(" src/ convex/ --include="*.ts" --include="*.tsx"
   - Result: 0 matches

3. No Jira write-back
   - Command: grep -r "PUT.*jira\|POST.*jira\|jira.*PUT\|jira.*POST" convex/ --include="*.ts" -i
   - Result: 1 text match in `convex/jira.ts`, but it is a false positive from the substring `input` + `JiraFetchResponse` on a type line.
   - Runtime audit: only Jira reads (`GET /field`, `POST /search/jql`) exist; no Jira write-back endpoint implementation found.

4. No dark mode toggle
   - Command: grep -r "dark-mode\|darkMode\|theme-toggle\|toggleTheme" src/ --include="*.tsx" --include="*.ts"
   - Result: 0 matches

5. No timer/countdown
   - Command: grep -r "countdown\|setInterval.*timer\|startTimer" src/ --include="*.tsx" --include="*.ts"
   - Result: 0 matches

6. No confetti/animations on reveal
   - Command: grep -r "confetti\|canvas-confetti\|party-js" src/ convex/ --include="*.ts" --include="*.tsx"
   - Result: 0 matches

7. No spectator mode
   - Command: grep -r "spectator\|observer\|viewer" src/ convex/ --include="*.ts" --include="*.tsx" -i
   - Result: 0 matches

8. No drag-and-drop
   - Command: grep -r "react-beautiful-dnd\|@dnd-kit\|react-dnd\|onDragStart\|onDragEnd" src/ --include="*.tsx"
   - Result: 0 matches

9. No export functionality
   - Command: grep -r "exportCSV\|exportPDF\|downloadReport\|toCsv\|jsPDF" src/ --include="*.tsx" --include="*.ts"
   - Result: 0 matches

10. No hardcoded story points field
   - Command: grep -r "customfield_10016" convex/ --include="*.ts"
   - Result: matches only in `convex/__tests__/jira.test.ts` fixture assertions.
   - Runtime audit: production code in `convex/jira.ts` discovers the field dynamically from `/rest/api/3/field`; no hardcoded field ID in runtime logic.

11. No "use node" directive
   - Command: grep -r '"use node"' convex/ --include="*.ts"
   - Result: match only in generated comment text within `convex/_generated/server.d.ts`; no directive in authored Convex functions.

12. No notification sounds
   - Command: grep -r "Audio(\|new Audio\|play()" src/ --include="*.tsx" --include="*.ts"
   - Result: 0 matches

13. No keyboard shortcuts library
   - Command: grep -r "hotkeys\|mousetrap\|tinykeys" src/ --include="*.tsx" --include="*.ts"
   - Result: 0 matches

SUMMARY
- Patterns checked: 13/13
- Runtime violations found: 0
- Acceptable false positives: 3
  * `convex/jira.ts` grep hit caused by `input` substring, not Jira write-back
  * `convex/__tests__/jira.test.ts` fixture uses `customfield_10016` to validate dynamic discovery behavior
  * `convex/_generated/server.d.ts` contains "use node" in generated documentation comment only

VERDICT: APPROVE
